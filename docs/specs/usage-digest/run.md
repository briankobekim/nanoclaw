# usage-digest — run record

- 2026-09-16 Brian: "Ok let's build that" (after the next-build recommendation). `/team-plan` started on `production` at a17db2e0. Design grounded by a read-only survey of the container→host channel, the SDK result fields, the scheduling module, the sweep, and the CLI resource pattern (findings folded into plan §3).
- Review budget by prior agreement: one cross-model plan review, one implementation review.

## Plan review (single round, as agreed) — revision 1
`completed` — exit 0; verdict `needs-attention`, 7 findings (4 high, 3 medium). Raw verdict at scratchpad `usage-plan-review/verdict.json`. All seven verified against source and accepted; one correction batch produced revision 2.

| # | Finding | Lead verification | Disposition (revision 2) |
|---|---|---|---|
| 1 | The record was written after `markCompleted`, so a crash in between lost the turn's record forever (high) | Confirmed: `poll-loop.ts:556` marks the batch complete first. | **MUST-FIX**: the record row is written before `markCompleted` and before any reply (C2 asserts the order). A crash between the write and completion re-runs the batch as a genuine second turn with its own row, which is the correct accounting; delivery retries dedupe on `(session_id, turn_id)`. |
| 2 | `notifyOwners` swallows every failure and returns normally with no adapter or DM, so the marker would be set with nobody notified; one global marker also loses per-owner retries (high) | Confirmed at `src/modules/memory-gate/notify.ts:22-53`. | **MUST-FIX**: digest-specific `sendDigestTo(owner, text)` that throws on any non-delivery; per-owner `usage_digest_deliveries` rows; window "since your last digest" so an outage yields a longer digest, not a skipped day; invariant relaxed to at-least-once (a crash inside the send/marker window may repeat once), mirroring the stall pinger. U5 rewritten around the real semantics. |
| 3 | Measurements are container-controlled and the `run_checks` analogy was wrong; payload strings could reach the owner DM (high) | Confirmed. | **MUST-FIX**: provider and model are host-derived (session row, container config), payload values ignored; charset, count, size and numeric bounds; consistency of `reported`/`usage`; a per-session rate cap; no payload string is ever rendered in the digest (invariant 8, U6). The unguarded justification now states what closes the boundary. |
| 4 | Failed turns: Codex ends a failed turn with an error and no `result`; `is_error` was not transmitted (high) | Confirmed at `providers/codex.ts:302-323` (`state.error` + `finishTurn`, no result). | **MUST-FIX**: one `recordTurn` path used by result events and by the terminal provider-failure path; `is_error` in the payload; C1 covers all four SDK error subtypes; new C3 for the no-result failure. |
| 5 | `recorded_at` was undefined and would default to host receipt time, misdating turns across midnight; the no-catch-up policy contradicted "every evening" (medium) | Confirmed. | **MUST-FIX**: `occurred_at` (container clock, validated within ±24 h of ingest) and `ingested_at`; summary and digest group by `occurred_at`; the digest window is "since the last digest", so nothing is skipped. U4 covers a retry across midnight. |
| 6 | No model for Codex turns (medium) | Confirmed. | **MUST-FIX**: `model` column populated from the group's container config for every row; `model_usage_json` remains the per-model breakdown when reported. |
| 7 | Invariants 3, 4, 6, 7 lacked cases (medium) | Confirmed. | **MUST-FIX**: C2 order, C3, U2 malformed-then-valid and rate cap, U5 per-owner and marker-failure, new U8 for invariant 7, U6 for invariant 8, U7 bounds. |

No second plan review (budget). Presented to Brian for approval as revision 2.

## Build (2026-09-16)
- Brian: "Build it" → plan revision 2 approved. Build started on `production` at a17db2e0; working tree clean apart from untracked local files unrelated to this feature.
- Ownership: container workstream (providers/types.ts, providers/claude.ts, poll-loop.ts, C1-C3) delegated to one builder with exclusive ownership; the lead owns the host module (migration, record action, sessions getter, wiring, U1-U3, U7); the CLI resource (U4) and the digest (U5, U6, U8) are delegated once the migration file, their shared interface, exists.
- Tests first: each workstream materializes its cases before implementing and records the initial failure.
- Plan correction during build (U2): `cost_usd: NaN` cannot cross the container→host boundary (the payload is `JSON.stringify`-ed, which renders NaN as `null`, and a null cost on a reported turn is legitimate when the SDK omits it). Replaced in plan §6 U2 by `cost_usd: -1` and `cost_usd: 20000`, both rejected by the handler's bounds. Product intent unchanged.
- Lead workstream done: `src/modules/usage/{migration,record,index}.ts`, `migration.test.ts` (U7), `record.test.ts` (U1-U3). Initial run of U1-U3 failed as expected with `Unknown system action record_usage` (5 cases); after implementation `vitest run src/modules/usage/record.test.ts src/modules/usage/migration.test.ts` → 2 files, 8 passed; ESLint clean on the three module files. Decisions: provider = `session.agent_provider` then the group's container config; model = the group's container config (`getContainerConfig`, no materialization); bounds and charset per §4.2; a stale `occurred_at` is replaced by ingest time with an info log; rate cap counts rows ingested in the trailing hour for the session.
- Rollout correction (plan §5, §7 step 8, §8): the agent image bakes dependencies only; `container/agent-runner/src` is bind-mounted read-only at `/app/src` and executed directly (`src/container-runner.ts:602-606,751`). No image rebuild; a host restart is the whole cutover. Product intent unchanged.
- CLI workstream done (delegated builder): `src/cli/resources/usage.ts` (virtual read-only resource, `summary --days` / `list --limit`, self-scoped, DST-safe local-day bucketing via `resolveGroupTimezone` + `formatLocalStamp`), `usage.test.ts` (U4, 7 cases), `index.ts` import. Initial run failed to load the missing module (expected). One line outside the builder's ownership was needed and added by the lead: `'usage'` in `GROUP_SCOPE_RESOURCES` (`src/cli/registry.ts`), without which the CLI guard denies group-scoped agents the resource. `vitest run src/cli/resources/usage.test.ts src/cli/help-render.test.ts src/cli/dispatch.test.ts` → 3 files, 73 passed; ESLint clean. Recorded deviations: summary rows also carry `agent_group_id` and `reported_turns`; a partially reported day renders `$x.xx partial (r of t turns)`; `list` omits `model_usage_json` (payload-derived keys) and adds the host-known agent name; an agent passing `--group <other>` is refused by the guard rather than silently pinned. None changes product intent.
- Digest workstream done (delegated builder): `src/modules/usage/digest.ts` (`usageDigestSweep` with injectable deps, per-owner marker rows, §4.4 text builder), `notify.ts` (`sendDigestTo`: resolves only on adapter acceptance; throws on no adapter, no DM, 30 s timeout or adapter error), `digest.test.ts` (U5 eight cases, U6 three, U8 one), `src/host-sweep.ts` hook, `src/config.ts` `DIGEST_HOUR` (`NANOCLAW_DIGEST_HOUR`, 0-23 or `off`, default 21), `src/db/sessions.ts` `listPendingApprovalsOpen`. Initial run failed to load the missing module (expected); two fixture failures (FK on deleting handoffs) fixed in the test only. `vitest run digest.test.ts host-sweep.test.ts host-sweep-grace.test.ts stall-ping.test.ts` → 4 files, 49 passed; typecheck clean; ESLint 0 errors (the new hook carries the same `no-catch-all` warning as the nine existing hooks). Recorded deviations: `sendDigestTo` takes an optional deps override for tests; handoff age is measured from `created_at`; no open handoffs renders `Handoffs: none`; a first send's window starts just before the earliest record (or now−24 h with none); an invalid `NANOCLAW_DIGEST_HOUR` falls back to 21 silently; cache-creation tokens are summed but not rendered (the §4.4 line omits them). None changes product intent.
- Container workstream done (delegated builder): `providers/types.ts` (`TurnUsage`, `usage?` on the result event), `providers/claude.ts` (`toTurnUsage`, populated on success and all four error subtypes; camelCase and snake_case per-model fields; negative/NaN tokens floor to 0, non-finite cost → null), `poll-loop.ts` (`recordTurn`: one `record_usage` system row per turn, written before `markCompleted` and any reply; also from the `processQuery` catch with `is_error:true`; a `turnPending` flag guarantees exactly one record per turn), `providers/claude-usage.test.ts` (C1, 7 cases), `poll-loop-usage.test.ts` (C2, C3, 5 cases). Initial runs: 0/7 and 0/5 (expected). Integration: the new bookkeeping row shifted the row counts asserted by four existing suites (28 cases); fixed in those tests only by filtering `kind:'system'` rows from their `getUndeliveredMessages` shim, no product change. Whole container tree in the image: 387 pass, 3 skip, 1 fail, 2 errors — the fail and both errors (`ENOENT /src/group-init.ts`, `extendTool: property "purpose"`) are identical on HEAD (375 pass there). Deviations recorded: a stream that ends with neither result nor exception (in-process abort only) is not recorded.
- Integration checks (lead): `pnpm typecheck` clean; ESLint 0 errors; full host suite → only the 4 pre-existing updater failures (2564 passed); `pnpm build` rebuilt. Files: 10 modified, 6 new host/container source and test files, plus the spec directory.

## Implementation review (single round, as agreed) — commit 92462c5b
`completed` — exit 0; verdict `needs-attention`, 3 findings (2 high, 1 medium). Raw verdict at scratchpad `usage-impl-review/verdict.json`.

| # | Finding | Lead verification | Disposition |
|---|---|---|---|
| 1 | Ordering: (a) a follow-up batch was marked complete at `query.push`, before its own result could record usage; (b) a streamed mid-turn reply goes out before the later result records (high) | (a) confirmed at the push seam; (b) confirmed by design of `emitsMidTurnText` providers. | **(a) Fixed**: batches are queued FIFO (`pendingBatches`; nudge pushes enqueue an empty batch so results and batches stay aligned) and a batch is completed only when its own result has been recorded; a crash before that re-runs it as a genuine second turn, the same semantics the initial batch already had. Covered by the existing follow-up suites (whole container tree green at baseline) plus inspection. **(b) Accepted residual**: buffering streamed replies would change the product, and pre-creating the record at the first streamed block needs an upsert path the host does not have; the exposure is a crash in the seconds between a streamed reply and its result, costing one under-counted turn. Recorded in plan §8. |
| 2 | A stream that ends with neither a result nor an exception (an abort during an active turn, e.g. `/clear`) recorded nothing although tokens were spent (high) | Confirmed: the only terminal path was the `catch`. | **Fixed**: on normal stream exhaustion with `turnPending` still set, one unreported error turn is recorded (write failures logged, never thrown). New C3 case: a generator that returns after `init` → one row `reported:false, is_error:true`. |
| 3 | The digest window compared `occurred_at` against the send-time cursor, so a record ingested after a send for a turn that occurred before it was skipped by every later digest (medium) | Confirmed. | **Fixed**: the window is an ingestion cursor (`ingested_at`), attribution and the summary's day grouping stay on `occurred_at`; the first window starts at the earliest `ingested_at`. New U5 case: a 20:59 turn ingested at 21:01 appears in the next evening's digest. |

Fresh checks after the batch: `pnpm typecheck` clean; `vitest run src/modules/usage` → 3 files, 21 passed; container tree in the image → 388 pass, 3 skip, 1 fail + 2 errors (identical to HEAD before this feature); full host suite → only the 4 pre-existing updater failures; ESLint 0 errors; build rebuilt. Review loop closed at one round by prior agreement (the reviewer asked for another; declined per the agreed budget, with finding 1(b) carried as the sole residual).
