# approval-click-durability — run record

- 2026-09-16 Brian: "Let's go with approval bridge fix and the two small things you suggested." Build started on `production` at 9e47872f.
- Review budget agreed in advance (see memory: cap adversarial review loops): one cross-model implementation review; fix only findings that are trust-boundary bypasses, data loss or silent corruption; record the rest as accepted risk.

## Implementation review (single round, as agreed)
`completed` — exit 0; verdict `needs-attention`, 4 findings (2 high, 2 medium). Raw verdict at scratchpad `click-durability-review/verdict.json`.

| # | Finding | Lead verification | Disposition |
|---|---|---|---|
| 1 | An UNAUTHORIZED click made the approvals handler return `true` (claimed) without changing the pending row, so both bridge paths terminalized the card over a still-pending approval, showing the stranger's selection (high) | Confirmed; the two permission-card handlers did the same. Pre-existing behaviour, but the new contract made it explicit. | **Fixed** (a stranded approval is the exact failure this build exists to remove): `ResponseOutcome = boolean \| 'refused'`; the approvals, sender-approval and channel-approval handlers return `'refused'` on an authorization rejection; the dispatcher forwards it; both bridge paths leave the card actionable on `'refused'`. Tests: synthetic `'refused'` recorders on both paths, plus an end-to-end case through the REAL approvals handler (a stranger's click leaves the row pending and the buttons in place; the owner's click is handled and the card terminalized). The response-handler unit test's expectation was updated from `true` to `'refused'`. |
| 2 | The pending-question handler writes the answer to the agent's inbox and only then deletes its row; two concurrent clicks can each append an answer, and a write-then-delete failure leaves the card actionable while the answer is already durable (medium) | Confirmed; pre-existing in the interactive module, and the plan overstated it. | **Accepted risk**, plan text corrected. The failure mode is a duplicate answer message to the agent, not a lost click; making the write idempotent is a separate change to the interactive module. |
| 3 | Discord: the deferred acknowledgement and the PATCH ignored non-2xx responses; a failed ack still records the click; a failed PATCH leaves a recorded click behind an actionable card with no retry (high) | Confirmed. | **Partly fixed, remainder accepted**: both calls now check `res.ok`; a failed ack throws inside its own try and is logged, and recording proceeds anyway (a recorded click with a "this interaction failed" toast beats an unrecorded one); a non-2xx PATCH is logged at error level with the status. No retry queue: a repeat click on the still-visible buttons is unclaimed (the row is gone) and terminalizes the card on that pass, so the state self-heals on the next tap. Discord is not in use on this host. Test: a 500 on the PATCH is logged, never thrown, and the click stays recorded. |
| 4 | The mission view mapped every closed row to stage `completed`, so an abandoned handoff looked finished (medium) | Confirmed. | **Fixed** (a silent misreport to the operator): a closed row whose closure evidence carries the `abandoned by operator:` prefix is staged `abandoned`, its state reads `closed: abandoned (<reason>)`, next action `none`. Test: abandoned from `created` and from `delivered` → `abandoned`, never `completed`; an untouched sibling stays `awaiting_delivery`. |

Fresh checks after the batch: `pnpm typecheck` clean; channels, approvals, permissions, interactive, memory-gate, CLI resources and host-core suites → 50 files, 441 tests passed; full suite → only the 4 pre-existing updater failures; ESLint 0 errors; build rebuilt. Review loop closed at one round by prior agreement.

## Rollout (2026-09-16)
- Host restarted onto 5aa32641 at 14:20 UTC-7 (marker stamped first; no crash loop; CLI socket up). One idle Atlas container was stopped by the restart; its session resumes on the next message.
- The four smoke-test handoffs left open by the revision-loop and verifier builds (`RECOVERY-20260909-ATLAS-ECHO`, `…675d2d37`, `…fe7ae416`, `…b2315abc`) were closed with `ncl handoffs abandon` from the host; open handoffs now 0; `ncl missions list` shows them as `abandoned` with the recorded reason (the one that a later round superseded keeps the `superseded` stage, with the abandonment in its state column).
- Live check still open for Brian: click any future approval card as the owner and confirm the buttons vanish only after the action lands; a click by anyone else now leaves the card untouched.
