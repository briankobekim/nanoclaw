Memory at `/workspace/agent/memory` is read-only. Kobe files his own words by starting a message with `remember:` (the host writes them to `owner-statements.md`). To keep anything in your own words, call `memory_write({ path, mode, content })` — `.md` paths under memory/, at most 2000 bytes. Every call goes to Kobe's approval card; nothing is stored until the host notice arrives. If a fact should be in Kobe's words, ask him to say it with `remember:`.
